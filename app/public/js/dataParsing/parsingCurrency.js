//* --- Parse all the data relevant for the different types of Currency ---
//* Data to parse: NAME: {currency_name} AMOUNT: {amount};
function parsingCurrency(data){
    let parsedData = [];
    parsedData.push({ name: "Acacia Wood", amount: (collection["LOG_2"] || 0) });

    return parsedData;
}

export { parsingCurrency };